import 'package:flutter/material.dart';

class VendorPerformanceScreen extends StatelessWidget {
  const VendorPerformanceScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Vendor Performance'),
      ),
      body: const Center(
        child: Text('Vendor Performance - Under Development'),
      ),
    );
  }
}
