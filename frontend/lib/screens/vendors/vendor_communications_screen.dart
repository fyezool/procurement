import 'package:flutter/material.dart';

class VendorCommunicationsScreen extends StatelessWidget {
  const VendorCommunicationsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Vendor Communications'),
      ),
      body: const Center(
        child: Text('Vendor Communications - Under Development'),
      ),
    );
  }
}
