import 'package:flutter/material.dart';

class VendorDirectoryScreen extends StatelessWidget {
  const VendorDirectoryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Vendor Directory'),
      ),
      body: const Center(
        child: Text('Vendor Directory - Under Development'),
      ),
    );
  }
}
