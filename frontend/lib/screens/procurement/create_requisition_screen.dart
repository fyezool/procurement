import 'package:flutter/material.dart';

class CreateRequisitionScreen extends StatelessWidget {
  const CreateRequisitionScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Create Requisition'),
      ),
      body: const Center(
        child: Text('Create Requisition - Under Development'),
      ),
    );
  }
}
