import 'package:flutter/material.dart';

class VendorReportsScreen extends StatelessWidget {
  const VendorReportsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Vendor Performance Reports'),
      ),
      body: const Center(
        child: Text('Vendor Performance Reports - Under Development'),
      ),
    );
  }
}
