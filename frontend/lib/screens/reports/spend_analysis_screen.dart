import 'package:flutter/material.dart';

class SpendAnalysisScreen extends StatelessWidget {
  const SpendAnalysisScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Spend Analysis'),
      ),
      body: const Center(
        child: Text('Spend Analysis - Under Development'),
      ),
    );
  }
}
